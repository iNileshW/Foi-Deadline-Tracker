# Runtime dependencies, fully pinned so pip-audit + Docker builds
# are reproducible. Regenerate with:
#
#     python -m venv /tmp/v && /tmp/v/bin/pip install flask gunicorn
#     /tmp/v/bin/pip freeze
#
# Bump direct deps deliberately; the transitives follow.

# --- direct ---
Flask==3.1.3
gunicorn==26.0.0

# --- transitive (Flask) ---
blinker==1.9.0
click==8.4.2
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
Werkzeug==3.1.8

# --- transitive (gunicorn) ---
packaging==26.2
